name: mars-demo-app-1
configuration:
  instances: 1
  environment:
    JAVA_OPTS: |
      -Dspring.cloud.config.enabled=true -Dspring.cloud.config.label=INTEGRATION
origin:
  container: <internal image registry>/mars/autoscaling-demo:2.0.12
# staging:
#   builder: paketobuildpacks/builder
